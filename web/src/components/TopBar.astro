---
import { site } from '../data/site';
---
<div class="topbar">
  <div class="wrap">
    <span class="urgent"><span class="dot"></span>Homes urgently needed — children across Ontario are waiting</span>
    <span class="links">
      <a href={`mailto:${site.newHomesEmail}`}>{site.newHomesEmail}</a>
      <a href={site.phoneHref}>{site.phone}</a>
    </span>
  </div>
</div>
